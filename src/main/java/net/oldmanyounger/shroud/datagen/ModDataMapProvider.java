package net.oldmanyounger.shroud.datagen;

/**
 * Placeholder data map provider for future Shroud datagen expansion.
 *
 * <p>This class currently exists as a stub so the project has an obvious place
 * to add NeoForge data map generation later if needed. It is intentionally empty
 * at the moment and is not actively wired into the generator flow by default.
 *
 * <p>In the broader context of the project, this file reserves space for future
 * content systems that may benefit from datamap generation without requiring the
 * surrounding datagen structure to be reorganized later.
 */
public class ModDataMapProvider {
}